// Placeholder. Overwritten at build time by scripts/generate-setup-code.mjs.
// A null hash means no setup code was baked in — the Worker then falls back to
// the documented open-but-clearly-unconfigured mode rather than locking the
// owner out of their own fresh deployment.
export const SETUP_CODE_HASH = null;
export const SETUP_CODE_GENERATED_AT = null;
